package com.universeview.utils.autotools.config;

public class AutoTool
{
	public static String getUpperFirst(String source)
	{	 
		return source.substring(0, 1).toUpperCase() + source.substring(1);
	}
}
